/*Create an abstract class Shape with an abstract method calculateArea()
calculateArea(). Implement two subclass: Circle and Square. Each subclass have
relevant attributes(like radius for circle,sidde for square) an
their own implementation of the calculateArea() method
*/

package Abstract;

public abstract class Shape83 {
    public abstract double calculateAreaofCircle();
}
