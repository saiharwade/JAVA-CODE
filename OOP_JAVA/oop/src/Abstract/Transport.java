//This is an Interface.

package Abstract;

public interface Transport {
//    public void getsetgo(){
//            System.out.println("hello");
//    };  in this example we wrote the body of the method, but that's wrong we can't write the body of the method in the interface class.

//   In interface each an every method is by default public it is not require to write down the access modifiers.
//   Only if you want to write the body of the method you can write it using default.



    //so
    void getsetgo(); //here we didn't wrote the body of the method
}
