package Abstract;

public interface Flyable {
    public abstract void fly();
    //The public and abstract is showing black cause in
    // the interface the method are by default public and abstract
}
